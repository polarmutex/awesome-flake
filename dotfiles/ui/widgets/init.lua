return {
    button = require(... .. '.button'),
    container = require(... .. '.container'),
    menu = require(... .. '.menu'),
    playerctl = require(... .. '.playerctl'),
    text = require(... .. '.text'),
    toggable_systray = require(... .. '.toggable_systray'),
}
